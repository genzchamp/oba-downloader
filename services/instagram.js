async function downloadInstagram(url) {

    return {
        success: false,
        platform: "Instagram",
        message: "Instagram downloader is temporarily unavailable while we upgrade it."
    };

}

module.exports = {
    downloadInstagram
};
